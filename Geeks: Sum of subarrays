class Solution {
  public:
    long long subarraySum(vector<int> &arr) {
        long long total = 0;
        int n = arr.size();
        for (int i = 0; i < n; i++) {
            total += 1LL * arr[i] * (i + 1) * (n - i);
        }
        return total;
    }
};
